# .env.example — placeholders for any secret or config names.
# Do not put real secrets here. Copy this to `.env` locally and fill real values.

SITE_TITLE=smallballgames
CONTACT_EMAIL=you@example.com
# Add any API keys or tokens here as names, but DO NOT commit real values to source control.
# e.g. MAPS_API_KEY=
